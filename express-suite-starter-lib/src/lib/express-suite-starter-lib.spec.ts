import { expressSuiteStarterLib } from './express-suite-starter-lib';

describe('expressSuiteStarterLib', () => {
  it('should work', () => {
    expect(expressSuiteStarterLib()).toEqual('express-suite-starter-lib');
  });
});
