import { expressSuiteExampleLib } from './express-suite-example-lib';

describe('expressSuiteExampleLib', () => {
  it('should work', () => {
    expect(expressSuiteExampleLib()).toEqual('express-suite-example-lib');
  });
});
