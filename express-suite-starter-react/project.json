{
  "name": "express-suite-starter-react",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "express-suite-starter-react/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project express-suite-starter-react --web",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "^build"
      ],
      "cache": true,
      "outputs": [
        "{workspaceRoot}/dist/express-suite-starter-react"
      ],
      "options": {
        "cwd": "express-suite-starter-react",
        "command": "vite build --mode development"
      },
      "configurations": {
        "development": {
          "command": "vite build --mode development"
        },
        "production": {
          "command": "vite build --mode production"
        }
      }
    }
  }
}
