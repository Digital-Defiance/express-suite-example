import { expressSuiteStarterApiLib } from './express-suite-starter-api-lib';

describe('expressSuiteStarterApiLib', () => {
  it('should work', () => {
    expect(expressSuiteStarterApiLib()).toEqual(
      'express-suite-starter-api-lib',
    );
  });
});
